# Rename this file to `.env` and fill with real credentials for local development.
# Never commit the populated `.env` file to version control.

DATABASE_URL=postgresql://user:password@host:port/database
